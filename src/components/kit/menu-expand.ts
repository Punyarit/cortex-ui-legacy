import { css, html, LitElement } from 'lit';
import { customElement } from 'lit/decorators.js';

@customElement('c-menu-expand')
export class MenuExpand extends LitElement {
  static styles = css``;

  render() {
    return html` <div>c-menu-expand component was created!!</div> `;
  }
}
